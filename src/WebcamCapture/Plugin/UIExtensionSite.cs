namespace WebcamCapture.Plugin
{
    /// <summary>
    /// Possible UI extension sites - where plugins can insert their own UI items.
    /// </summary>
    public enum UIExtensionSite
    {
        /// <summary>
        /// "Options" menu.
        /// </summary>
        Options
    }
}